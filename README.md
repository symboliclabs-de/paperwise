# Paperwise

Paperwise is an agentic document analysis pipeline. It reads a document, determines what type it is, extracts the relevant fields, and validates the result.

If fields are missing after the initial read, Paperwise goes back and searches for them specifically iterating until everything is found or a threshold is reached.


Built by [Symbolic Labs](https://github.com/symboliclabs-de)